/// @description Insert description here
// You can write your code in this editor
x = lerp(x, xstart, 0.2);

if( room = Room1)
{
	room_persistent = false;
	room_goto(MainMenu);
	audio_stop_all()
	instance_destroy();
}